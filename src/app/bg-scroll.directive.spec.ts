import { BgScrollDirective } from './bg-scroll.directive';

describe('BgScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new BgScrollDirective();
    expect(directive).toBeTruthy();
  });
});
